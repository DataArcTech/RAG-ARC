OPENAI_API_KEY=sk-
OPENAI_BASE_URL=https://api.gptsapi.net/v1

# Unified parser output directory (all parsers output to subdirectories here)
# Structure: {PARSER_OUTPUT_DIR}/native/, {PARSER_OUTPUT_DIR}/dots_ocr/, {PARSER_OUTPUT_DIR}/vlm_ocr/
PARSER_OUTPUT_DIR=./data/parsed_files

# Legacy individual parser directories (deprecated, use PARSER_OUTPUT_DIR instead)
# DOTSOCR_OUTPUT_DIR=./dotsorc/output
# NATIVE_PARSER_OUTPUT_DIR=./native/output
# VLMOCR_OUTPUT_DIR=./vlmocr/output

LOCAL_FILE_STORAGE_PATH=./local/files
HF_TOKEN=xxx

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5555
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123
POSTGRES_DB=rag_arc

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# # Run:
# openssl rand -hex 32
# to generate a key
JWT_SECRET_KEY=x x x