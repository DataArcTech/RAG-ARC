{
  "type": "deepsearch_tool_mcp_server",
  "instructions": "DeepSearch tool MCP server exposing a curated subset of DeepSearch tools (override via DEEPSEARCH_TOOL_MCP_TOOLS or enabled_tools).",
  "inherit_from_deepsearch_service": true,
  "tool_manager": {
    "audit_label": "${DEEPSEARCH_TOOL_MCP_AUDIT_LABEL}"
  },
  "scope": {
    "scope_id": "${DEEPSEARCH_TOOL_MCP_SCOPE_ID}",
    "scope_type": "owner",
    "labels": [],
    "attributes": {}
  }
}
