# RAG-ARC 项目理解指南

> 从底层到上层的完整架构解析

## 📋 目录

1. [项目概述](#项目概述)
2. [整体架构图](#整体架构图)
3. [API接口流程](#api接口流程)
4. [从底到上理解项目](#从底到上理解项目)

---

## 项目概述

**RAG-ARC** 是一个模块化的检索增强生成（RAG）框架，支持多路径检索、图结构提取和融合排序。

### 核心能力

- 🧩 **RAG 全流程支持**：文档解析 → 分块向量化 → 多路径检索 → 重排序 → LLM生成
- 🔍 **多路径检索融合**：BM25 + Dense + GraphRAG，使用RRF融合
- 🌐 **GraphRAG支持**：基于HippoRAG2，支持子图PPR，增量更新
- 👥 **多用户隔离**：支持多用户，数据完全隔离

---

## 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        API层 - 对外接口                          │
│  FastAPI路由  │  WebSocket  │  MCP服务器                        │
└──────────────┼─────────────┼────────────────────────────────────┘
               │             │
               ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     应用层 - 业务逻辑                            │
│  RAGInference  │  Knowledge  │  Account  │  ChatSession         │
└───────────────┼─────────────┼───────────┼──────────────────────┘
                │             │           │
                ▼             ▼           ▼
┌─────────────────────────────────────────────────────────────────┐
│                     核心层 - 核心能力                            │
│  QueryRewrite  │  MultiPathRetriever  │  Reranker              │
│  FileManagement  │  IndexManager                                 │
└───────────────┼──────────────┼───────────┼──────────────────────┘
                │              │           │
                ▼              ▼           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   封装层 - 接口抽象                              │
│  LLM接口  │  数据库接口  │  向量数据库  │  图数据库  │  文件存储 │
└───────────┼──────────────┼─────────────┼────────────┼──────────┘
            │              │             │            │
            ▼              ▼             ▼            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   框架层 - 基础设施                              │
│  AbstractModule  │  AbstractConfig  │  Register                │
└──────────────────┼──────────────────┼──────────────────────────┘
                   │                  │
                   ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                   配置层 - 配置管理                              │
│  JSON配置文件 → 环境变量替换 → 配置验证                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## API接口流程

> **层级标注说明**：
> - `[API]` = API层
> - `[应用]` = 应用层
> - `[核心]` = 核心层
> - `[封装]` = 封装层
> - `[框架]` = 框架层

### 1. 用户认证接口

#### POST /auth/register - 用户注册
```
客户端 → 
  API路由(auth.py) [API] → 
  Account模块 [应用] → 
  UserStorage [核心] → 
  PostgreSQL [封装] → 
  users表 [数据存储]
```

#### POST /auth/token - 用户登录
```
客户端 → 
  API路由(auth.py) [API] → 
  authenticate_user [API] → 
  Account模块 [应用] → 
  UserStorage [核心] → 
  PostgreSQL [封装] → 
  users表 [数据存储] → 
  验证密码 [应用] → 
  生成JWT [API] → 
  返回token
```

---

### 2. 知识库管理接口

#### POST /knowledge - 上传文件
```
客户端 → 
  API路由(knowledge.py) [API] → 
  get_current_user [API] → 
  Knowledge模块 [应用] → 
  FileStorage [核心] → 
  ├→ MetadataStore [封装] → PostgreSQL [封装] → file_metadata表 [数据存储]
  └→ BlobStore [封装] → MinIO/Local文件系统 [数据存储]
  
后台异步索引:
Knowledge模块 [应用] → 
  IndexManager [核心] → 
  ├→ Parser [核心] → 解析文档 → PostgreSQL [封装] → parsed_content表 [数据存储]
  ├→ Chunker [核心] → 分块文本 → PostgreSQL [封装] → chunks表 [数据存储]
  └→ Indexers [核心] → 
      ├→ FAISS索引 [封装] → FAISS索引文件 [数据存储]
      ├→ BM25索引 [封装] → Tantivy索引文件 [数据存储]
      └→ GraphStore [封装] → Neo4j/NetworkX [数据存储]
```

#### GET /knowledge/{file_id}/download - 下载文件
```
客户端 → 
  API路由(knowledge.py) [API] → 
  get_current_user [API] → 
  Knowledge模块 [应用] → 
  FileStorage [核心] → 
  ├→ MetadataStore [封装] → PostgreSQL [封装] → file_metadata表 [数据存储]
  └→ BlobStore [封装] → MinIO/Local文件系统 [数据存储] → 
  返回文件流
```

#### DELETE /knowledge/{file_id} - 删除文件
```
客户端 → 
  API路由(knowledge.py) [API] → 
  get_current_user [API] → 
  Knowledge模块 [应用] → 
  ├→ IndexManager [核心] → 
  │   ├→ FAISS索引 [封装] → 删除向量索引
  │   ├→ BM25索引 [封装] → 删除全文索引
  │   ├→ GraphStore [封装] → 删除图节点
  │   └→ PostgreSQL [封装] → 删除chunks表、parsed_content表记录
  ├→ FileStorage [核心] → MetadataStore [封装] → PostgreSQL [封装] → 删除file_metadata表记录
  └→ FileStorage [核心] → BlobStore [封装] → MinIO/Local文件系统 [数据存储] → 删除文件
```

#### GET /knowledge/list_files - 列出文件
```
客户端 → 
  API路由(knowledge.py) [API] → 
  get_current_user [API] → 
  Knowledge模块 [应用] → 
  FileStorage [核心] → 
  MetadataStore [封装] → PostgreSQL [封装] → file_metadata表 [数据存储] → 
  返回文件列表
```

#### POST /knowledge/trigger_indexing - 触发索引
```
客户端 → 
  API路由(knowledge.py) [API] → 
  get_current_user [API] → 
  Knowledge模块 [应用] → 
  FileStorage [核心] → MetadataStore [封装] → PostgreSQL [封装] → 验证file_id和owner_id → 
  IndexManager [核心] → 后台异步索引(同上传文件的索引流程)
```

---

### 3. RAG推理接口

#### POST /rag_inference/chat - RAG对话
```
客户端 → 
  API路由(rag_inference.py) [API] → 
  get_current_user [API] → 
  RAGInference模块 [应用] → 
  ├→ QueryRewriter [核心] → LLM接口 [封装] → OpenAI API [外部服务]
  ├→ MultiPathRetriever [核心] → 
  │   ├→ DenseRetriever [核心] → FAISS索引 [封装] → FAISS索引文件 [数据存储]
  │   ├→ TantivyBM25 [核心] → Tantivy索引 [封装] → Tantivy索引文件 [数据存储]
  │   └→ GraphRetriever [核心] → Neo4j图数据库 [封装] → Neo4j [数据存储]
  │   └→ RRF融合 [核心] → 融合多个检索结果
  ├→ Reranker [核心] → LLM接口 [封装] → OpenAI API [外部服务]
  └→ LLM接口 [封装] → OpenAI API [外部服务] → 生成回答
  
可选: return_subgraph=True → 
  _generate_mindmap [应用] → 
  LLM接口 [封装] → OpenAI API [外部服务] → 生成思维导图
```

#### WebSocket /rag_inference/stream_chat/{session_id} - 流式对话
```
客户端 → 
  WebSocket连接 → 
  API路由(rag_inference.py) [API] → 
  ws_get_current_user [API] → 
  ChatSessionManager [应用] → 
  ChatSessionStorage [核心] → PostgreSQL [封装] → chat_sessions表 [数据存储] → 验证session → 
  
循环处理消息:
  用户消息 → 
    ChatMessageManager [应用] → 
    ChatMessageStorage [核心] → 
    ├→ Redis [封装] → Redis缓存 [数据存储]
    └→ PostgreSQL [封装] → chat_messages表 [数据存储]
  
  历史消息 → 
    ChatMessageStorage [核心] → 
    ├→ Redis [封装] → Redis缓存 [数据存储] [优先从缓存读取]
    └→ PostgreSQL [封装] → chat_messages表 [数据存储] [缓存未命中时查询]
  
  RAGInference模块 [应用] → (同POST /rag_inference/chat流程)
  
  助手消息 → 
    ChatMessageManager [应用] → 
    ChatMessageStorage [核心] → 
    ├→ Redis [封装] → Redis缓存 [数据存储]
    └→ PostgreSQL [封装] → chat_messages表 [数据存储]
  
  → WebSocket推送响应
```

---

### 4. 会话管理接口

#### POST /session - 创建会话
```
客户端 → 
  API路由(session.py) [API] → 
  get_current_user [API] → 
  ChatSessionManager [应用] → 
  ChatSessionStorage [核心] → 
  PostgreSQL [封装] → chat_sessions表 [数据存储] → 返回session_id
```

#### GET /session - 列出会话
```
客户端 → 
  API路由(session.py) [API] → 
  get_current_user [API] → 
  ChatSessionManager [应用] → 
  ChatSessionStorage [核心] → 
  PostgreSQL [封装] → chat_sessions表 [数据存储] → 返回会话列表
```

#### POST /session/{session_id}/messages - 创建消息
```
客户端 → 
  API路由(session.py) [API] → 
  get_current_user [API] → 
  ChatSessionManager [应用] → 
  PostgreSQL [封装] → chat_sessions表 [数据存储] → 验证session和user_id → 
  ChatMessageManager [应用] → 
  ChatMessageStorage [核心] → 
    ├→ Redis [封装] → Redis缓存 [数据存储]
    └→ PostgreSQL [封装] → chat_messages表 [数据存储] → 返回消息
```

#### GET /session/{session_id}/messages - 列出消息
```
客户端 → 
  API路由(session.py) [API] → 
  get_current_user [API] → 
  ChatSessionManager [应用] → 
  PostgreSQL [封装] → chat_sessions表 [数据存储] → 验证session和user_id → 
  ChatMessageManager [应用] → 
  ChatMessageStorage [核心] → 
    ├→ Redis [封装] → Redis缓存 [数据存储] [优先从缓存读取]
    └→ PostgreSQL [封装] → chat_messages表 [数据存储] [缓存未命中时查询] → 返回消息列表
```

---

### 5. 用户信息接口

#### GET /user/me - 获取当前用户信息
```
客户端 → 
  API路由(user.py) [API] → 
  get_current_user [API] → 
  Account模块 [应用] → 
  UserStorage [核心] → 
  PostgreSQL [封装] → users表 [数据存储] → 返回用户信息
```

---

## 从底到上理解项目

### 第一层：框架层（Foundation Layer）

**位置**: `framework/`

**核心组件**：
- `AbstractModule`: 所有模块的基类
- `AbstractConfig`: 所有配置类的基类，强制实现`build()`方法
- `Register`: 单例注册系统，加载JSON配置并构建模块实例

**理解要点**：
- 定义了所有模块必须遵循的规范
- 配置驱动：JSON配置文件 + 环境变量 → 构建模块实例

---

### 第二层：封装层（Encapsulation Layer）

**位置**: `encapsulation/`

**主要模块**：
- `database/`: 数据库接口（PostgreSQL、FAISS、Neo4j、Redis、MinIO等）
- `llm/`: LLM接口（OpenAI、Qwen等）
- `data_model/`: 数据模型定义

**理解要点**：
- 隔离外部依赖，上层代码不直接依赖具体实现
- 通过Config选择具体实现，易于切换

---

### 第三层：核心层（Core Layer）

**位置**: `core/`

**核心模块**：
- `file_management/`: 文档解析、分块、索引构建
- `retrieval/`: 检索策略（Dense、BM25、Graph、MultiPath）
- `query_rewrite/`: 查询重写
- `rerank/`: 重排序

**理解要点**：
- 实现RAG系统的核心算法和能力
- 每个模块职责单一，可组合使用

---

### 第四层：应用层（Application Layer）

**位置**: `application/`

**主要模块**：
- `rag_inference/`: RAG推理核心，组合查询重写、检索、重排序、LLM生成
- `knowledge/`: 知识库管理，处理文档上传和索引
- `account/`: 用户账户和会话管理

**理解要点**：
- 组合核心层能力成完整业务功能
- 每个应用模块对应一个Config类

---

### 第五层：API层（API Layer）

**位置**: `api/`

**主要路由**：
- `rag_inference.py`: RAG对话接口
- `knowledge.py`: 知识库管理接口
- `session.py`: 会话管理接口
- `auth.py`: 认证接口

**理解要点**：
- 提供HTTP和WebSocket接口
- 通过Register获取应用层模块实例
- 处理认证、授权、请求响应

---

## 数据存储总结

| 数据类型 | 存储位置 | 说明 |
|---------|---------|------|
| 用户信息 | PostgreSQL `users` 表 | 用户注册、登录验证 |
| 文件元数据 | PostgreSQL `file_metadata` 表 | 文件基本信息、状态 |
| 文件内容 | MinIO/Local文件系统 | 实际文件存储 |
| 解析内容 | PostgreSQL `parsed_content` 表 | 文档解析结果 |
| 文本块 | PostgreSQL `chunks` 表 | 分块后的文本 |
| 向量索引 | FAISS索引文件 | 向量相似度检索 |
| BM25索引 | Tantivy索引文件 | 全文检索 |
| 图索引 | Neo4j/NetworkX | 知识图谱 |
| 会话信息 | PostgreSQL `chat_sessions` 表 | 对话会话 |
| 消息内容 | PostgreSQL `chat_messages` 表 + Redis缓存 | 对话消息 |

---

## 总结

RAG-ARC采用**分层架构**设计：
1. **框架层**：定义规范和基础设施
2. **封装层**：抽象外部依赖
3. **核心层**：实现核心算法和能力
4. **应用层**：组合核心能力成业务功能
5. **API层**：提供对外接口

**核心设计理念**：
- **配置驱动**：JSON配置 + 环境变量组装系统
- **模块化**：职责单一，易于扩展和测试
- **接口抽象**：上层不依赖具体实现，易于切换
- **用户隔离**：支持多用户，数据完全隔离
